# ЦУП Альтаир v6 / 0.6.1

Дата исправленного релиза: 27 августа 2026 г.

## Основной пакет телеметрии

Канонический пакет v6:

```text
02,00001,00015,3.00,4.20,1,33
```

Длина пакета — 29 ASCII-символов. Поля имеют фиксированную длину и разделяются запятыми:

```text
ID(2), PACKET(5), UPTIME(5), PANEL_POWER(4), VOLT(4), MODE(1), CHECKSUM(2 HEX)
```

Контрольная сумма XOR рассчитывается по всей строке перед двумя HEX-символами контрольной суммы, включая последнюю запятую. Для канонического примера XOR равен `0x33`.

`MODE=1` означает штатный режим, `MODE=0` — аварийный. В рабочей бортовой логике аварийный режим должен автоматически включаться при напряжении аккумулятора ниже 2.00 В.

## Радиоканал CC1101

Профиль v6:

```text
Частота:       435.000 МГц
Модуляция:     2-FSK
Bit rate:      4.8 kbps
Девиация:      5 кГц
RX bandwidth:  58 кГц
TX power:      5 dBm
Preamble:      16 bit
Encoding:      NRZ
Sync word:     0x12AD
Packet length: variable
CRC:           ON
```

## Передатчик STM32

В каталоге `firmware/transmitter_stm32_v6` находится пример передатчика STM32 + CC1101, который раз в секунду передаёт статический канонический пакет v6 на 435.000 МГц.

## Наземный шлюз ESP32 — исправление 0.6.1

Стендовая проверка подтвердила, что STM32 + CC1101 IntroSat и обычный CC1101 + ESP32 аппаратно совместимы: простой приёмник RadioLib принял полный пакет `02,00001,00015,3.00,4.20,1,33` длиной 29 символов.

В 0.6.1 сложный ESP32 gateway переведён на тот же надёжный путь приёма: GDO0 (GPIO4) опрашивается напрямую, а `NRZ`, sync word `0x12AD`, variable packet length и CRC задаются явно. После приёма ESP32 проверяет XOR и передаёт в ЦУП нормализованную строку `$TEL` с RSSI, LQI и диагностической оценкой SNR.

## Windows-приложение

Версия приложения: `0.6.1`.

Имя установщика:

```text
CUP-Altair-Setup-0.6.1.exe
```

GitHub Release v0.6.1 должен содержать установщик, `Transmit_v6_static.ino`, исправленную прошивку ESP32, manifest встроенного прошивальщика и `TELEMETRY_V6.md`.

Подробности стендового исправления приведены в `docs/RELEASE_V6_1_RX_FIX.md`.
